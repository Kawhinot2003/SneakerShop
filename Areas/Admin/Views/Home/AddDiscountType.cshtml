@model SneakerShop.Models.Entities.DiscountType
@using SneakerShop.Models.Entities
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <fieldset>
        @Html.HiddenFor(m => m.Id)
        <p>
            @Html.LabelFor(m => m.Name, "Название")
            <br />
            @Html.EditorFor(m => m.Name)
        </p>
        <p>
            @Html.LabelFor(m => m.Description, "Описание")
            <br />
            @Html.EditorFor(m => m.Description)
        </p>
        <p>
            @Html.LabelFor(m => m.Percent, "Процент")
            <br />
            @Html.EditorFor(m => m.Percent)
        </p>

        <p><input type="submit" value="Отправить" /></p>
    </fieldset>
}